info:
  name: Get specific user avatar
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/users/:id/avatar"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
