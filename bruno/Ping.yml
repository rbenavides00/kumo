info:
  name: Ping
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/ping"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
