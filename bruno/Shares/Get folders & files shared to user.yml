info:
  name: Get folders & files shared to user
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/shares/shared-with-me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
