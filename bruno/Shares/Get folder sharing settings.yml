info:
  name: Get folder sharing settings
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/shares/folder/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
