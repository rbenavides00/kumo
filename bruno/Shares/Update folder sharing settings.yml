info:
  name: Update folder sharing settings
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{url}}/shares/folder/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "isPublic": true,
        "sharedWith": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
