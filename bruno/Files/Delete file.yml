info:
  name: Delete file
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/files/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
