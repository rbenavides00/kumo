info:
  name: Download file
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/files/:id/download"
  params:
    - name: id
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
