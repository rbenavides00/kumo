info:
  name: Get specific folder contents
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/folders/:id/contents"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
